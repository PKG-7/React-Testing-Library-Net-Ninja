import { render, screen } from '@testing-library/react';
import Header from '../Header';

// он отдает то, что ему передали в пропсы title
// он отдает header и он один
it('он отдает то, что ему передали в пропсы title', () => {});

it('он отдает header и он один', () => {});
